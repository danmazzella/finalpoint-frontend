import { AppRegistry } from 'react-native';
import App from './App';

AppRegistry.registerComponent('FinalPointApp', () => App);
AppRegistry.runApplication('FinalPointApp', {
  rootTag: document.getElementById('root'),
}); 